# stop the existing console consumers by Ctrl+C
# Launch a new command prompt
Start -> cmd
cd /code/kafkacode-main/kafkacode-main/
# Extract the 5advancede_consumer_manualcommit.zip
cd 5advancede_consumer_manualcommit/5advancede_consumer_manualcommit/kafka-consumer/
mvn package
cd target
dir
docker cp kafka-consumer-0.0.1-SNAPSHOT-jar-with-dependencies.jar multinodekafka-kafka1-1:/advancedconsumer.jar
docker exec -it multinodekafka-kafka1-1 bash
java -jar advancedconsumer.jar 

# on another terminal get into the container again
docker exec -it multinodekafka-kafka1-1 bash
java -jar advancedconsumer.jar 

# on another terminal & start the producer jar
docker exec -it multinodekafka-kafka1-1 bash

# run the producer jar again to create new messages for the consumer to consume
java -jar *-jar-with-dependencies.jar  first_topic
java -jar *-jar-with-dependencies.jar  first_topic
java -jar *-jar-with-dependencies.jar  first_topic
